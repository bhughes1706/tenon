node_modules/
dist/
.env
*.local

# data dir (sqlite db + photos) — backed up separately
data/

# OS
.DS_Store
Thumbs.db

# Local verification screenshots (regenerate via the puppeteer recipe in AGENT_HANDOFF.md)
docs/screenshots/

# Vite
packages/web/dist/
packages/web/.vite/

# pnpm
.pnpm-store/

# TypeScript incremental build cache
*.tsbuildinfo

# Build artifacts
*.tgz
tenon.tar.gz
releases/

# Tailscale certs (contain private key)
*.pem
